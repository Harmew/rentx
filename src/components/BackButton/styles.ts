import styled from "styled-components/native";

// React Native Gesture Handler is a library that provides a cross-platform API for creating native gestures using the power of the JavaScript thread.
import { BorderlessButton } from "react-native-gesture-handler";

// Allow the BackButton component to be styled using the styled-components library and the BorderlessButton component from the React Native Gesture Handler library.
export const Container = styled(BorderlessButton)``;
